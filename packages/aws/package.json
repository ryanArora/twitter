{
  "name": "@repo/aws",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "typecheck": "tsc --noEmit"
  },
  "eslintConfig": {
    "extends": "@repo/eslint-config"
  },
  "dependencies": {
    "aws-sdk": "^2.1540.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "@types/node": "^20.11.0"
  }
}
